멀티 에이전트 문서 자동화 시스템 사용 가이드
환경 구성

D:\dev\ptlpsmback  ← 백엔드 (Spring Boot)
D:\dev\ptlpsm      ← 프론트엔드 (Nuxt 3) + 에이전트 시스템

1. 기본 사용법
1.1 전체 매뉴얼 자동 생성

# 프론트엔드 디렉토리에서 실행
cd D:\dev\ptlpsm

# Claude Code에서 커맨드 실행
/generate-manual
실행 결과:

docs/generated/
├── USER_MANUAL.md          # 사용자 매뉴얼
├── API_DOCUMENTATION.md    # API 문서
├── STATUS_FLOWS.md         # 상태 흐름도
├── BUSINESS_RULES.md       # 비즈니스 규칙
├── ENTITY_RELATIONSHIPS.md # 엔티티 관계도
├── entities/               # 엔티티별 문서
│   ├── Order.md
│   ├── Shipment.md
│   └── ...
└── index.md               # 목차
1.2 코드 분석만 실행

/analyze-codebase           # 전체 분석
/analyze-codebase frontend  # 프론트엔드만
/analyze-codebase api       # API만
1.3 특정 섹션만 생성

/generate-section entity Order      # Order 엔티티 문서
/generate-section api shipment      # 출하 API 문서
/generate-section flow Baseline     # 기성차수 상태 흐름
1.4 문서 검증 및 수정

/validate-docs              # 검증만
/validate-docs --fix        # 자동 수정 포함
/validate-docs --verbose    # 상세 보고서


2. 백엔드 연동 설정
현재 백엔드 분석기는 스텁 모드입니다. 백엔드를 연동하려면:
2.1 backend-analyzer.md 수정
.claude/agents/analyzers/backend-analyzer.md 파일에서:

# 활성화 설정 추가
backend_config:
  enabled: true
  repository_path: "D:/dev/ptlpsmback"
  source_root: "src/main/java"
  package_prefix: "com.platree.ptlpsm"
2.2 백엔드 분석 대상

D:\dev\ptlpsmback\src\main\java\
├── entity/          # JPA 엔티티
├── repository/      # Repository
├── service/         # 서비스 레이어
├── controller/      # REST 컨트롤러
├── dto/            # DTO
└── enums/          # 상태 Enum
3. 실제 사용 시나리오
시나리오 1: 처음 문서 생성

# Step 1: 코드 분석
/analyze-codebase

# Step 2: 결과 확인
# .claude/shared/data/frontend-analysis.yaml 확인

# Step 3: 전체 문서 생성
/generate-manual

# Step 4: 품질 검증
/validate-docs

# Step 5: 자동 수정 적용
/validate-docs --fix
시나리오 2: 코드 변경 후 문서 업데이트

# types/order.ts 수정 후

# 1. 프론트엔드만 재분석
/analyze-codebase frontend

# 2. Order 문서만 재생성
/generate-section entity Order

# 3. 검증
/validate-docs
시나리오 3: 새 엔티티 추가 후

# 새 엔티티 추가 후

# 1. Knowledge Base 업데이트
# .claude/knowledge-base/terminology.yaml에 새 엔티티 추가

# 2. 전체 재분석 및 생성
/generate-manual
4. Knowledge Base 관리
4.1 용어 추가 (terminology.yaml)
새 엔티티나 필드 추가 시 .claude/knowledge-base/terminology.yaml 수정:

entities:
  NewEntity:
    korean: "새엔티티"
    table: "new_entities"
    description: "새로운 엔티티 설명"

fields:
  newField: "새필드"
4.2 상태 추가 (status-mappings.yaml)
새 상태 흐름 추가 시 .claude/knowledge-base/status-mappings.yaml 수정:

new_entity:
  entity: "NewEntity"
  states:
    PENDING:
      korean: "대기"
      is_initial: true
    COMPLETED:
      korean: "완료"
      is_final: true
  transitions:
    - from: PENDING
      to: COMPLETED
      trigger: "처리 완료"
4.3 비즈니스 규칙 추가 (domain-rules.yaml)
새 규칙 추가 시 .claude/knowledge-base/domain-rules.yaml 수정:

relationships:
  parent_to_child:
    type: ONE_TO_MANY
    korean: "1건에 여러 건 가능"
    constraint: "수량 제약 조건"
5. 출력 파일 구조
5.1 분석 결과 (런타임 생성)

.claude/shared/data/
├── frontend-analysis.yaml      # 프론트엔드 분석 결과
├── api-analysis.yaml           # API 분석 결과
├── backend-analysis.yaml       # 백엔드 분석 결과
├── database-analysis.yaml      # DB 분석 결과
├── synthesized-knowledge.yaml  # 통합 지식 모델
└── validation-report.yaml      # 검증 보고서
5.2 생성된 문서

docs/generated/
├── USER_MANUAL.md              # 사용자 매뉴얼
├── API_DOCUMENTATION.md        # API 문서
├── STATUS_FLOWS.md             # 상태 흐름도
├── BUSINESS_RULES.md           # 비즈니스 규칙
├── ENTITY_RELATIONSHIPS.md     # 엔티티 관계
├── entities/                   # 엔티티별 상세
└── index.md                    # 목차
6. 워크플로우 요약

┌─────────────────────────────────────────────────────────────┐
│                      일반 워크플로우                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   코드 변경  ──→  /analyze-codebase  ──→  /generate-manual  │
│                                               │             │
│                                               ▼             │
│                                      /validate-docs --fix   │
│                                               │             │
│                                               ▼             │
│                                        docs/generated/      │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      부분 업데이트                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   특정 파일 수정  ──→  /analyze-codebase frontend           │
│                              │                              │
│                              ▼                              │
│                    /generate-section entity Order           │
│                              │                              │
│                              ▼                              │
│                       /validate-docs                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘


6. 상태 흐름도

[계약 등록]
    │
    ▼
┌─────────────────────────────────────┐
│  선급금 신청 가능                      │ ◀── 서명 완료된 출하 0개
│  (출하 유무 상관없이)                   │
└─────────────────────────────────────┘
    │
    │ 출하 후 인수증 서명 완료 (1개라도)
    ▼
┌─────────────────────────────────────┐
│  선급금 신청 불가                     │
│  기성금 청구 가능                     │ ◀── isDeliveryCompleted = false
└─────────────────────────────────────┘
    │
    │ 납품완료서 생성 (PENDING_SIGNATURE)
    ▼
┌─────────────────────────────────────┐
│  기성금 청구 가능                     │
│  잔금/납품완료 청구 가능               │ ◀── status = PENDING_SIGNATURE
└─────────────────────────────────────┘
    │
    │ 납품완료 서명 완료 (COMPLETED)
    │ → isDeliveryCompleted = true 자동 업데이트
    ▼
┌─────────────────────────────────────┐
│  기성금 청구 불가                     │
│  잔금/납품완료 청구 불가               │
│  → "잔금등록" 버튼으로 변경            │
└─────────────────────────────────────┘
7. 요약 테이블
버튼	활성화 조건	비활성화 시 처리
선급금 신청	completedDeliveryCount === 0	툴팁 표시
기성금 청구	!isDeliveryCompleted	툴팁 표시
납품완료 청구	status === 'PENDING_SIGNATURE'	툴팁 또는 버튼 텍스트 변경


7. 주의사항
Knowledge Base 우선: 코드 분석 결과보다 terminology.yaml, status-mappings.yaml, domain-rules.yaml이 우선합니다.
백엔드 스텁 모드: 현재 백엔드 분석기는 별도 저장소 접근이 설정되지 않아 knowledge-base를 fallback으로 사용합니다.
수동 작업 필요 (20%):
스크린샷 추가
사용자 시나리오 작성
복잡한 다이어그램 조정
분석 결과 보존: .claude/shared/data/ 파일들은 git에서 제외하는 것을 권장합니다.
8. 빠른 시작

# 1. 첫 실행 - 전체 문서 생성
/generate-manual

# 2. 품질 확인
/validate-docs --verbose

# 3. 문제 자동 수정
/validate-docs --fix

# 4. 결과 확인
# docs/generated/ 폴더의 문서 확인


  Phase 1: 초기화

  ✓ 공유 데이터 디렉토리 준비됨
  ✓ Knowledge Base 확인됨 (3개 파일)

  Phase 2: 분석 (병렬 실행)

  ├── Frontend Analyzer: 18개 엔티티, 55개 페이지
  ├── API Analyzer: 198개 엔드포인트
  ├── Backend Analyzer: (스텁 모드)
  └── Database Analyzer: (스텁 모드)

  Phase 3: 지식 통합

  ✓ 18개 엔티티 통합
  ✓ 5개 상태 흐름 정의
  ✓ 12개 비즈니스 규칙 추출
  ✓ 신뢰도: 88%

  Phase 4: 문서 생성

  ✓ USER_MANUAL.md (21KB)
  ✓ API_DOCUMENTATION.md (16KB)
  ✓ STATUS_FLOWS.md (17KB)
  ✓ BUSINESS_RULES.md (11KB)
  ✓ ENTITY_RELATIONSHIPS.md (13KB)
  ✓ index.md (6KB)
  ✓ 7개 엔티티 문서
  ✓ 3개 워크플로우 문서
  ✓ 1개 API 참조 문서
  ✓ 1개 역할 문서
  ✓ 1개 PDF 문서 가이드

  Phase 5: 품질 검증

  📊 품질 점수: 92/100 (A등급)
  - 완전성: 95
  - 일관성: 90
  - 정확성: 94
  - 가독성: 88

  ---
  📁 생성된 파일 목록

  docs/generated/
  ├── USER_MANUAL.md          # 사용자 매뉴얼 (메인)
  ├── API_DOCUMENTATION.md    # API 문서
  ├── STATUS_FLOWS.md         # 상태 흐름도
  ├── BUSINESS_RULES.md       # 비즈니스 규칙
  ├── ENTITY_RELATIONSHIPS.md # 엔티티 관계도
  ├── index.md                # 문서 인덱스
  ├── entities/
  │   ├── order.md
  │   ├── shipment.md
  │   ├── transport.md
  │   ├── delivery.md
  │   ├── baseline.md
  │   ├── fund.md
  │   └── delivery-done.md
  ├── workflows/
  │   ├── main-flow.md
  │   ├── signature-flow.md
  │   └── fund-flow.md
  ├── api/
  │   └── api-reference.md
  ├── roles/
  │   └── user-roles.md
  └── documents/
      └── pdf-documents.md

  ---
  ⚠️ 발견된 문제

  | 유형 | 건수 | 내용                                   |
  |------|------|----------------------------------------|
  | 오류 | 0    | -                                      |
  | 경고 | 5    | API 응답 예시 부족, 스크린샷 미포함 등 |

  ---
  👉 다음 단계

  1. /validate-docs --fix - 자동 수정 실행
  2. 스크린샷 추가 (수동) - 화면 캡처 후 문서에 삽입
  3. FAQ 작성 (수동) - 현장 피드백 기반