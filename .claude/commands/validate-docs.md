---
description: 생성된 문서의 품질을 검증하고 선택적으로 자동 수정을 적용합니다.
argument-hint: [--fix] [--category <category>] [--verbose]
allowed-tools: Read, Write, Edit, Grep, Glob
---

# /validate-docs 커맨드

생성된 문서의 품질을 검증하고 문제점을 보고합니다.

## 사용법

```bash
/validate-docs                      # 검증만 실행
/validate-docs --fix                # 자동 수정 포함
/validate-docs --category accuracy  # 특정 카테고리만
/validate-docs --verbose            # 상세 보고서
/validate-docs --fix --verbose      # 자동 수정 + 상세
```

## 옵션

- `--fix`: 자동 수정 가능한 문제 수정
- `--category <category>`: 특정 카테고리만 검증
  - `completeness`: 완전성
  - `consistency`: 일관성
  - `accuracy`: 정확성
  - `readability`: 가독성
- `--verbose`: 상세 보고서 출력

---

## 검증 카테고리

### 1. 완전성 (Completeness) - 30%

```yaml
checks:
  - 모든 엔티티 문서화 여부
  - 필수 섹션 존재 여부
  - API 엔드포인트 누락 여부
  - 상태 흐름도 존재 여부
```

### 2. 일관성 (Consistency) - 25%

```yaml
checks:
  - 용어 통일성 (terminology.yaml 기준)
  - 포맷 일관성 (테이블, 헤더)
  - 파일 명명 규칙
  - 섹션 순서
```

### 3. 정확성 (Accuracy) - 30%

```yaml
checks:
  - 상태 값이 status-mappings.yaml과 일치
  - API 경로가 엔드포인트 정의와 일치
  - 필드 타입이 TypeScript 정의와 일치
  - 비즈니스 규칙이 domain-rules.yaml과 일치
```

### 4. 가독성 (Readability) - 15%

```yaml
checks:
  - 내부 링크 유효성
  - 다이어그램 너비 (80자 이하)
  - 논리적 섹션 구조
  - 빈 섹션 없음
```

---

## 자동 수정 가능 항목

### 수정 가능 (--fix 옵션)

```yaml
auto_fixable:
  terminology:
    - "영문 단독 사용 → 한글(영문) 형식"
    - "동사형 → 명사형"

  formatting:
    - "테이블 구분선 통일"
    - "필수 아이콘 통일 (✓/-)"

  missing_content:
    - "상태 흐름도 자동 생성"
    - "누락된 API 엔드포인트 추가"
```

### 수동 필요

```yaml
manual_required:
  - "스크린샷 추가"
  - "사용자 시나리오 작성"
  - "복잡한 다이어그램 수정"
  - "도메인 전문 지식 필요 내용"
```

---

## 출력 예시

### 기본 검증

```
🔍 문서 품질 검증 중...

📊 품질 점수: 92/100
   - 완전성: 95
   - 일관성: 90
   - 정확성: 94
   - 가독성: 88

✓ 78개 검사 통과
⚠️ 5개 경고
❌ 2개 오류

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 오류:

[VAL-E001] completeness
  📄 docs/generated/entities/Transport.md
  📍 상태 흐름 섹션
  💬 상태 흐름 다이어그램 누락
  💡 /generate-section flow Transport 실행

[VAL-E002] accuracy
  📄 docs/generated/API_DOCUMENTATION.md
  📍 출하 관리 API
  💬 DELETE /admin/shipments/{id} 엔드포인트 누락
  💡 /generate-section api shipment 실행

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ 경고:

[VAL-W001] consistency
  📄 docs/generated/API_DOCUMENTATION.md:145
  💬 '삭제'와 'Delete' 혼용
  💡 /validate-docs --fix 로 자동 수정 가능

[VAL-W002] readability
  📄 docs/generated/USER_MANUAL.md
  📍 3. 업무 흐름
  💬 다이어그램이 80자 너비 초과
  💡 수동 조정 필요

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 권장 조치:
1. /validate-docs --fix     # 자동 수정 (3건)
2. 수동 수정 필요 (2건)

📁 상세 보고서: .claude/shared/data/validation-report.yaml
```

### --fix 옵션 사용

```
🔧 자동 수정 모드 실행 중...

수정 전 점수: 92/100
수정 대상: 3건

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ [VAL-W001] 수정됨
  📄 API_DOCUMENTATION.md:145
  📝 'Delete' → '삭제'

✓ [VAL-E001] 수정됨
  📄 entities/Transport.md
  📝 상태 흐름도 생성됨

✓ [VAL-E002] 수정됨
  📄 API_DOCUMENTATION.md
  📝 DELETE 엔드포인트 추가됨

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

수정 후 점수: 97/100 (+5)
✓ 3건 자동 수정 완료
⚠️ 2건 수동 수정 필요

📋 남은 수동 작업:
1. 다이어그램 너비 조정 (USER_MANUAL.md)
2. 비즈니스 규칙 상세화 (entities/Baseline.md)
```

### --verbose 옵션

```
🔍 상세 검증 보고서

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                    완전성 검사 (COMPLETENESS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

엔티티 문서:
  ✓ Order.md - 모든 섹션 존재
  ✓ Shipment.md - 모든 섹션 존재
  ✓ Transport.md - 상태 흐름 누락 ❌
  ✓ Delivery.md - 모든 섹션 존재
  ✓ Baseline.md - 모든 섹션 존재
  ✓ Fund.md - 모든 섹션 존재

API 문서:
  ✓ 44/45 엔드포인트 문서화
  ❌ DELETE /admin/shipments/{id} 누락

상태 흐름:
  ✓ Order - 존재
  ✓ Shipment - 존재
  ❌ Transport - 누락
  ✓ Delivery - 존재
  ✓ Baseline - 존재
  ✓ Fund - 존재

점수: 95/100

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                    일관성 검사 (CONSISTENCY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

용어 검사:
  ✓ 148/150 용어 일치
  ⚠️ 2건 불일치
    - API_DOCUMENTATION.md:145 'Delete' → '삭제'
    - entities/Order.md:78 'Status' → '상태'

포맷 검사:
  ✓ 테이블 형식 일관
  ✓ 헤더 형식 일관
  ✓ 아이콘 형식 일관

점수: 90/100

... (계속)
```

### --category 옵션

```
/validate-docs --category accuracy

🔍 정확성 검증 중...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                    정확성 검사 (ACCURACY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

상태 값 검증:
  ✓ Order 상태 - 일치
  ✓ Shipment 상태 - 일치
  ✓ Baseline 상태 - 일치
  ✓ Fund 상태 - 일치
  소스: status-mappings.yaml

API 경로 검증:
  ✓ 44/45 경로 일치
  ❌ 1건 불일치
    - DELETE /admin/shipments/{id}
  소스: api-analysis.yaml

필드 타입 검증:
  ✓ 모든 필드 타입 일치
  소스: types/**/*.ts

점수: 94/100
```

---

## 검증 보고서 파일

검증 결과는 YAML 파일로 저장됩니다:

**경로:** `.claude/shared/data/validation-report.yaml`

```yaml
generated_at: "2026-01-03T10:00:00Z"
validator: "quality-validator"

summary:
  total_checks: 85
  passed: 78
  warnings: 5
  errors: 2
  quality_score: 92

score_breakdown:
  completeness: 95
  consistency: 90
  accuracy: 94
  readability: 88

issues:
  - id: VAL-E001
    severity: ERROR
    category: completeness
    document: "entities/Transport.md"
    message: "상태 흐름 다이어그램 누락"
    auto_fixable: true
  # ... 추가 이슈

next_steps:
  auto_fixable:
    - issue_id: VAL-E001
      command: "/generate-section flow Transport"
  manual_required:
    - issue_id: VAL-W002
      action: "다이어그램 수동 조정"
```

---

## 품질 등급

```
95+ : 우수 (Excellent)
85-94: 양호 (Good)
70-84: 보통 (Acceptable)
<70  : 개선 필요 (Needs Improvement)
```

---

## 관련 커맨드

- `/generate-manual` - 전체 문서 생성
- `/generate-section` - 특정 섹션 재생성
- `/analyze-codebase` - 코드베이스 재분석
