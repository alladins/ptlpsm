---
description: 'Git add, commit, push를 한 번에 실행합니다'
allowed-tools:
  [
    'Bash(git add:*)',
    'Bash(git status:*)',
    'Bash(git commit:*)',
    'Bash(git diff:*)',
    'Bash(git log:*)',
    'Bash(git push:*)',
    'Bash(git branch:*)',
  ]
---

# Claude 명령어: Commit & Push

변경된 파일을 스테이징하고, 컨벤셔널 커밋 메시지를 생성한 후, 원격 저장소에 푸시합니다.

## 사용법

```
/commit-push
```

## 프로세스

1. `git status`로 변경된 파일 확인
2. 변경된 파일 전체 또는 선택적으로 `git add`
3. `git diff --staged`로 스테이지된 변경사항 분석
4. 이모지 컨벤셔널 포맷으로 커밋 메시지 생성
5. `git commit` 실행
6. `git push` 실행 (현재 브랜치로)

## 커밋 포맷

`<이모지> <타입>: <설명>`

**타입:**

- `feat`: 새로운 기능
- `fix`: 버그 수정
- `docs`: 문서화
- `style`: 포맷팅
- `refactor`: 코드 리팩토링
- `perf`: 성능 개선
- `test`: 테스트
- `chore`: 빌드/도구

**규칙:**

- 명령형 어조 ("추가" not "추가됨")
- 첫 줄 72자 미만
- 원자적 커밋 (단일 목적)

## 이모지 맵

✨ feat | 🐛 fix | 📝 docs | 💄 style | ♻️ refactor | ⚡ perf | ✅ test | 🔧 chore | 🚀 ci | 🚨 warnings | 🔒️ security | 🚚 move | 🏗️ architecture | ➕ add-dep | ➖ remove-dep | 🧑‍💻 dx | 🏷️ types | 👔 business | 🚸 ux | 🩹 minor-fix | 🔥 remove | 🎨 structure | 🚑️ hotfix

## 주의사항

- 푸시 전 원격 브랜치와 충돌 여부 확인
- 충돌 시 사용자에게 알림
- **커밋에 Claude 서명 절대 추가하지 않음**
- force push는 절대 하지 않음
