---
description: 전체 사용자 매뉴얼을 자동 생성합니다. 4계층 멀티에이전트 파이프라인을 실행합니다.
argument-hint: [output-format] [target-entity]
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, Task
---

# /generate-manual 커맨드

전체 문서 생성 파이프라인을 오케스트레이션합니다.

## 사용법

```bash
/generate-manual                    # 전체 생성, Markdown 형식
/generate-manual html               # 전체 생성, HTML 출력 (후처리)
/generate-manual markdown Order     # Order 엔티티만 생성
```

## 인자

- `$1` (output-format): `markdown` (기본값), `html`, `pdf`
- `$2` (target-entity): `all` (기본값) 또는 특정 엔티티명

---

## 실행 흐름

```
┌────────────────────────────────────────────────────────────────┐
│                    Phase 1: 초기화                              │
│  - 공유 데이터 디렉토리 생성                                     │
│  - Knowledge Base 존재 확인                                     │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│                    Phase 2: 분석 (병렬)                         │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │  Frontend    │ │     API      │ │   Backend    │ (stub)     │
│  │  Analyzer    │ │   Analyzer   │ │   Analyzer   │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
│                  ┌──────────────┐                               │
│                  │   Database   │ (stub)                        │
│                  │   Analyzer   │                               │
│                  └──────────────┘                               │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│                    Phase 3: 통합 (순차)                         │
│                  ┌──────────────────────┐                       │
│                  │ Knowledge Synthesizer │                      │
│                  └──────────────────────┘                       │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│                    Phase 4: 생성 (순차)                         │
│                  ┌──────────────────────┐                       │
│                  │  Document Generator  │                       │
│                  └──────────────────────┘                       │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│                    Phase 5: 검증 (순차)                         │
│                  ┌──────────────────────┐                       │
│                  │  Quality Validator   │                       │
│                  └──────────────────────┘                       │
└────────────────────────────┬───────────────────────────────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│                    Phase 6: 보고                                │
│  - 생성 결과 요약                                               │
│  - 품질 점수 표시                                               │
│  - 다음 단계 안내                                               │
└────────────────────────────────────────────────────────────────┘
```

---

## 실행 단계

### Step 1: 초기화

```bash
# 공유 데이터 디렉토리 생성
mkdir -p .claude/shared/data

# Knowledge Base 확인
if [ ! -f ".claude/knowledge-base/terminology.yaml" ]; then
  echo "Error: terminology.yaml not found"
  exit 1
fi
```

### Step 2: 분석기 실행 (병렬)

4개의 분석기 에이전트를 병렬로 실행합니다:

1. **frontend-analyzer**
   - 입력: `types/**/*.ts`, `pages/**/*.vue`, `components/**/*.vue`
   - 출력: `.claude/shared/data/frontend-analysis.yaml`

2. **api-analyzer**
   - 입력: `services/api/endpoints/**/*.ts`, `services/*.service.ts`
   - 출력: `.claude/shared/data/api-analysis.yaml`

3. **backend-analyzer** (스텁)
   - 입력: Knowledge Base (fallback)
   - 출력: `.claude/shared/data/backend-analysis.yaml`

4. **database-analyzer** (스텁)
   - 입력: Knowledge Base (fallback)
   - 출력: `.claude/shared/data/database-analysis.yaml`

### Step 3: 지식 통합

**knowledge-synthesizer** 실행:
- 입력: 모든 분석 결과 + Knowledge Base
- 출력: `.claude/shared/data/synthesized-knowledge.yaml`

### Step 4: 문서 생성

**document-generator** 실행:
- 입력: `synthesized-knowledge.yaml`
- 출력: `docs/generated/`

생성되는 파일:
```
docs/generated/
├── USER_MANUAL.md
├── API_DOCUMENTATION.md
├── STATUS_FLOWS.md
├── BUSINESS_RULES.md
├── ENTITY_RELATIONSHIPS.md
├── entities/
│   ├── Order.md
│   ├── Shipment.md
│   ├── Transport.md
│   ├── Delivery.md
│   ├── Baseline.md
│   ├── Fund.md
│   └── ...
└── index.md
```

### Step 5: 품질 검증

**quality-validator** 실행:
- 입력: 생성된 문서 + `synthesized-knowledge.yaml`
- 출력: `.claude/shared/data/validation-report.yaml`

### Step 6: 결과 보고

```markdown
## 생성 완료

**품질 점수:** 92/100

### 생성된 문서:
- USER_MANUAL.md (245 lines)
- API_DOCUMENTATION.md (180 lines)
- STATUS_FLOWS.md (95 lines)
- 6개 엔티티 문서

### 발견된 문제:
- 1 error (수동 수정 필요)
- 2 warnings (자동 수정 가능)

### 다음 단계:
1. `/validate-docs --fix`로 자동 수정 실행
2. 스크린샷 추가 (수동)
3. 사용자 시나리오 작성 (수동)
```

---

## 에러 처리

```yaml
errors:
  knowledge_base_missing:
    message: "Knowledge Base 파일을 찾을 수 없습니다"
    files:
      - terminology.yaml
      - status-mappings.yaml
      - domain-rules.yaml
    action: "파일 생성 후 재실행"

  analysis_failed:
    message: "분석 단계 실패"
    action: "개별 분석기 로그 확인"

  synthesis_failed:
    message: "지식 통합 실패"
    action: "분석 결과 파일 확인"

  generation_failed:
    message: "문서 생성 실패"
    action: "synthesized-knowledge.yaml 확인"
```

---

## 출력 예시

### 성공 시
```
🚀 문서 생성 시작...

Phase 1: 초기화
✓ 공유 데이터 디렉토리 준비됨
✓ Knowledge Base 확인됨

Phase 2: 분석 (병렬 실행)
├── Frontend Analyzer: 15개 엔티티, 55개 페이지
├── API Analyzer: 45개 엔드포인트
├── Backend Analyzer: (스텁 모드)
└── Database Analyzer: (스텁 모드)

Phase 3: 지식 통합
✓ 15개 엔티티 통합
✓ 9개 상태 흐름 정의
✓ 12개 비즈니스 규칙 추출

Phase 4: 문서 생성
✓ USER_MANUAL.md 생성됨
✓ API_DOCUMENTATION.md 생성됨
✓ 6개 엔티티 문서 생성됨

Phase 5: 품질 검증
📊 품질 점수: 92/100
   - 완전성: 95
   - 일관성: 90
   - 정확성: 94
   - 가독성: 88

⚠️ 2개 경고, 0개 오류

📁 생성된 파일: docs/generated/
```

### 부분 성공 시
```
⚠️ 문서 생성 완료 (일부 문제 발견)

📊 품질 점수: 78/100

❌ 오류:
1. Transport 상태 흐름도 누락

⚠️ 경고:
1. 용어 불일치 2건
2. API 엔드포인트 1건 누락

👉 해결 방법:
   /validate-docs --fix     # 자동 수정 실행
   /generate-section flow Transport  # 특정 섹션 재생성
```

---

## 관련 커맨드

- `/analyze-codebase` - 분석만 실행
- `/generate-section` - 특정 섹션만 생성
- `/validate-docs` - 문서 검증 및 수정
